#ifndef TYPEDEFS_H_
#define TYPEDEFS_H_

#include <stdint.h>

typedef unsigned char ubyte;
typedef unsigned int uint;
typedef unsigned short light_t; // 16-bit packed light data (R, G, B, S each 4 bits)

#endif /* TYPEDEFS_H_ */

